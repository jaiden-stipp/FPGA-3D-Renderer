# Model Assets

`low_poly_crystal.obj` is the small default viewer model.

`monkey.obj` is an export of Blender's built-in Suzanne test mesh. It is included as a stress test for loading, simplification, chunking, upload, and rendering. The renderer does not use OBJ material or texture files.
